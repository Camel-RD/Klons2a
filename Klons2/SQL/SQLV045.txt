SET TERM ^ ;

ALTER PROCEDURE SP_M_DEL_FINDOC (
    PIDDOCM INTEGER)
AS
DECLARE VARIABLE VDOCID INT;
BEGIN
    FOR SELECT F_OPSD.ID
    FROM F_OPSD
    WHERE (F_OPSD.IDDOCM IS NOT NULL) AND (F_OPSD.IDDOCM = :PIDDOCM)
    INTO :VDOCID
    DO
    BEGIN
        DELETE FROM F_OPS WHERE F_OPS.DOCID = :VDOCID;
    END
    DELETE FROM F_OPSD
    WHERE (F_OPSD.IDDOCM IS NOT NULL) AND (F_OPSD.IDDOCM = :PIDDOCM);
END
^

SET TERM ; ^


UPDATE OR INSERT INTO M_DOCTYPES (ID, CODE, NAME, OPTYPE)
   VALUES (18, 'Iepirkuma rēķ.', 'Iepirkuma rēķins', 0);
UPDATE OR INSERT INTO M_DOCTYPES (ID, CODE, NAME, OPTYPE)
   VALUES (19, 'Pārdošanas rēķ.', 'Pārdošanas rēķins', 0);
UPDATE OR INSERT INTO M_DOCTYPES (ID, CODE, NAME, OPTYPE)
   VALUES (20, 'Avansa rēķ.', 'Avansa rēķins', 0);
UPDATE OR INSERT INTO M_DOCTYPES (ID, CODE, NAME, OPTYPE)
   VALUES (21, 'Kredītrēķ. vienk.', 'Kredītrēķins vienkāršs', 0);


UPDATE OR INSERT INTO M_PVNRATES2 (IDRATE, IDTP, IDTRTP,
        BASE_DEB_FIN, BASE_CRED_FIN, BASE_CRED_PVN,
        PVN_DEB_FIN, PVN_CRED_FIN, PVN_CRED_PVN,
        IDPVNTEXT)
VALUES (1, 2, 19, 3, 5, '104', 3, 1, '204', 1)
MATCHING (IDRATE, IDTP, IDTRTP);

UPDATE OR INSERT INTO M_PVNRATES2 (IDRATE, IDTP, IDTRTP,
        BASE_DEB_FIN, BASE_CRED_FIN, BASE_CRED_PVN,
        PVN_DEB_FIN, PVN_CRED_FIN, PVN_CRED_PVN,
        IDPVNTEXT)
VALUES (1, 2, 20, 3, 5, '104', 3, 1, '204', 1)
MATCHING (IDRATE, IDTP, IDTRTP);

UPDATE OR INSERT INTO M_PVNRATES2 (IDRATE, IDTP, IDTRTP,
        BASE_DEB_FIN, BASE_CRED_FIN, BASE_CRED_PVN,
        PVN_DEB_FIN, PVN_CRED_FIN, PVN_CRED_PVN,
        IDPVNTEXT)
VALUES (1, 2, 21, 3, 5, '104', 3, 1, '204', 1)
MATCHING (IDRATE, IDTP, IDTRTP);



UPDATE OR INSERT INTO M_PVNRATES2 (IDRATE, IDTP, IDTRTP,
        BASE_DEB_FIN, BASE_CRED_FIN, BASE_CRED_PVN,
        PVN_DEB_FIN, PVN_CRED_FIN, PVN_CRED_PVN,
        IDPVNTEXT)
VALUES (3, 2, 19, 3, 5, '104', 3, 1, '204', 1)
MATCHING (IDRATE, IDTP, IDTRTP);

UPDATE OR INSERT INTO M_PVNRATES2 (IDRATE, IDTP, IDTRTP,
        BASE_DEB_FIN, BASE_CRED_FIN, BASE_CRED_PVN,
        PVN_DEB_FIN, PVN_CRED_FIN, PVN_CRED_PVN,
        IDPVNTEXT)
VALUES (3, 2, 20, 3, 5, '104', 3, 1, '204', 1)
MATCHING (IDRATE, IDTP, IDTRTP);

UPDATE OR INSERT INTO M_PVNRATES2 (IDRATE, IDTP, IDTRTP,
        BASE_DEB_FIN, BASE_CRED_FIN, BASE_CRED_PVN,
        PVN_DEB_FIN, PVN_CRED_FIN, PVN_CRED_PVN,
        IDPVNTEXT)
VALUES (3, 2, 21, 3, 5, '104', 3, 1, '204', 1)
MATCHING (IDRATE, IDTP, IDTRTP);



UPDATE Params
SET PVALUE = '045'
WHERE PNAME = 'version';
